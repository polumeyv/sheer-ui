---
'sheer-ui': patch
---

Without CSS anchor positioning (iOS Safari 18, Firefox ESR 140) popovers, tooltips, menus and selects center on screen with a fitted box instead of rendering at the top-left or edge to edge.
