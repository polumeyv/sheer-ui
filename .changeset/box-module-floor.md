---
'sheer-ui': patch
---

The box module is down to `boxWith`, `repairBindable` and two structural types (`{ current }`); `bindableWith`, the unused `Box`/`MaybeGetter`/`get` and the symbol branding are gone. No public change.
