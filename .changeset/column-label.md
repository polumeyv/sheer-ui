---
'sheer-ui': minor
---

`ColumnDef.label` names a column for people: `Column.label` falls back to the id, `DataTableViewOptions` lists columns by it instead of the capitalized raw id, and `sortHeader` renders a sort button carrying it.
