---
'sheer-ui': patch
---

NavigationMenu `skipDelayDuration` now works: after a hover menu closes, entering another trigger within that window opens after the short in-menu delay instead of the full `delayDuration`.
