// The showcase is a dev tool: render client-side only so we never have to
// worry about SSR-ing 70 browser-oriented (portal/floating/focus) components.
export const ssr = false;
export const prerender = false;
